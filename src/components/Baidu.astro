---
import { siteConfig } from '@/config';

const { enabled, siteId } = siteConfig.services.baiduAnalytics;
---

{ enabled && (
  <script is:inline async src={`https://hm.baidu.com/hm.js?${siteId}`}>
  </script>
) }
